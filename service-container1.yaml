apiVersion: v1
kind: Service
metadata:
  name: service-container1
spec:
  type: LoadBalancer
  selector:
    app: test-assignment
    component: container1
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
 
